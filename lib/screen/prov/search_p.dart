import 'package:flutter/material.dart';

class Search_p extends StatefulWidget {
  const Search_p({Key? key}) : super(key: key);

  @override
  _Search_pState createState() => _Search_pState();
}

class _Search_pState extends State<Search_p> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
