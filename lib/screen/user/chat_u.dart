import 'package:flutter/material.dart';

class Chat_u extends StatefulWidget {
  const Chat_u({Key? key}) : super(key: key);

  @override
  _ChatState createState() => _ChatState();
}

class _ChatState extends State<Chat_u> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("แชท"),
      ),
    );
  }
}
