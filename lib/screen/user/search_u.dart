import 'package:flutter/material.dart';

class Search_u extends StatefulWidget {
  const Search_u({Key? key}) : super(key: key);

  @override
  _Search_uState createState() => _Search_uState();
}

class _Search_uState extends State<Search_u> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ค้นหา"),
      ),
    );
  }
}
